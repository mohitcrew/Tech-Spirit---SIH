import { Body, Controller, Get, Post, Req, UseGuards } from '@nestjs/common'; import { AuthGuard } from '@nestjs/passport'; import { AuthService } from './auth.service'; import { LoginDto,RegisterDto } from './dto/auth.dto';
@Controller('auth') export class AuthController {constructor(private s:AuthService){} @Post('register') register(@Body()d:RegisterDto){return this.s.register(d)} @Post('login') login(@Body()d:LoginDto){return this.s.login(d)} @UseGuards(AuthGuard('jwt')) @Get('me') me(@Req()r:any){return this.s.me(r.user.id)} @Post('logout') logout(){return {message:'Logged out'}}}
